<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name="com.hilingual.presentation.widget.topic.RecommendedTopicWidgetReceiver"
            android:exported="true"
            android:label="@string/recommended_topic_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recommended_topic_widget_info" />
        </receiver>

        <receiver
            android:name="com.hilingual.presentation.widget.streak.StreakWidgetReceiver"
            android:exported="true"
            android:label="@string/streak_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/streak_widget_info" />
        </receiver>
    </application>

</manifest>

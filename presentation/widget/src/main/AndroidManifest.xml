<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name="com.hilingual.presentation.widget.topic.RecommendedTopicSmallWidgetReceiver"
            android:exported="true"
            android:label="@string/recommended_topic_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recommended_topic_widget_info_s" />
        </receiver>

        <receiver
            android:name="com.hilingual.presentation.widget.topic.RecommendedTopicLargeWidgetReceiver"
            android:exported="true"
            android:label="@string/recommended_topic_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recommended_topic_widget_info_l" />
        </receiver>

        <receiver
            android:name="com.hilingual.presentation.widget.streak.StreakSmallWidgetReceiver"
            android:exported="true"
            android:label="@string/streak_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/streak_widget_info_s" />
        </receiver>

        <receiver
            android:name="com.hilingual.presentation.widget.streak.StreakLargeWidgetReceiver"
            android:exported="true"
            android:label="@string/streak_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/streak_widget_info_l" />
        </receiver>
    </application>

</manifest>
